.env
__pycache__/
*.pyc
onchain/devnet_keys/

# 포기한 pay.sh CLI 클론 (자체 .git 보유 — nested repo 충돌 방지 위해 제외)
onchain/pay/

# Kaggle 원본 데이터 + 전처리 산출물 (용량/재배포 라이선스 문제로 제외, README에 재현 방법 안내 예정)
Training Data.csv
Test Data.csv
Sample Prediction Dataset.csv
processed/

# 모델 버전 스냅샷/후보 바이너리 — 이력은 model_registry.json(추적됨)에 남기고,
# 실제 .joblib 스냅샷은 로컬에만 쌓아둔다 (버전마다 커밋하면 저장소가 계속 불어남)
models/versions/
models/_candidate/

# Claude Code 로컬 설정 (개인 환경용, 공개 repo에 불필요)
.claude/

# 발표용 PPT 초안 (GitHub에 올리지 않음)
deck/

